@model IEnumerable<Nutripocket.Models.PERIODICIDADE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESC_PERIODO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RECOR_ALIMENTACAO.DESC_ALIMENTO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESC_PERIODO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RECOR_ALIMENTACAO.DESC_ALIMENTO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PERIODO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PERIODO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PERIODO })
        </td>
    </tr>
}

</table>
